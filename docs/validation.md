# Collection checks

- 101 catalog entries and unique SHA-256 values.
- All source images decoded successfully; dimensions and byte lengths recorded.
- Original downloaded bytes retained; hashes rechecked against catalog.
- Exact byte/pixel duplicates removed, followed by visual review of contact sheets to exclude repeated size/language variants.
- Gallery preview encodings, documentation and material helpers excluded from asset count.
- Categories distinguish reusable file types from visual references.
- Provenance and rights notes supplied per entry.
- Three large images use the publisher’s medium-large rendition; each retains a link to its full-resolution source in the catalog.
- All 101 uploaded asset Git blob hashes and byte sizes match the local catalog files.
